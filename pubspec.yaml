name: combofox
description: "ComboFox — a mobile and web app for arcade fighting-game fans to view move lists."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+10

environment:
  sdk: ^3.10.8

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  firebase_core: ^4.6.0
  firebase_app_check: ^0.4.1+4
  cloud_firestore: ^6.2.0
  firebase_storage: ^13.2.0
  firebase_auth: ^6.3.0
  cached_network_image: ^3.3.0
  image_picker: ^1.0.0
  connectivity_plus: ^7.2.0
  phosphor_flutter: ^2.1.0
  url_launcher: ^6.3.2
  flutter_svg: ^2.2.4
  go_router: ^17.2.1
  share_plus: ^13.2.0
  shared_preferences: ^2.5.5
  package_info_plus: ^10.2.0
  google_fonts: ^8.1.0
  flutter_native_splash: ^2.4.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3

# phosphor_flutter 2.1.0 extends Flutter's `IconData`, which was marked
# final in Flutter 3.43. Pin to PR #62's fork until upstream publishes a fix.
# Remove this override once https://github.com/phosphor-icons/flutter/pull/62
# is merged and a new version is released on pub.dev.
dependency_overrides:
  phosphor_flutter:
    git:
      url: https://github.com/subarnaPdl/phosphor_icons.git
      ref: "514f0a883571c4453dbd6ee3041d1dcba4fb8fc7"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/foxxy/
    - assets/foxxy/sd/
    - assets/foxxy/loadingscreens/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  fonts:
    - family: Doto
      fonts:
        - asset: assets/fonts/Doto-VariableFont.ttf

flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: "assets/images/combofox-icon.png"
  min_sdk_android: 21
  adaptive_icon_background: "#0B0F1A"
  adaptive_icon_foreground: "assets/images/combofox-icon.png"
  adaptive_icon_foreground_inset: 20
  web:
    generate: true
    image_path: "assets/images/combofox-icon.png"

flutter_native_splash:
  color: "#0B0F1A"
  android: true
  ios: true
  web: true
  android_12:
    color: "#0B0F1A"
